server.port=8087
spring.application.name=ms-combate

# Base de datos propia de ms-combate
spring.datasource.url=jdbc:mysql://localhost:3306/combate_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true

# JWT
jwt.secret=RealmOfValyronSecretKeyParaJWT2024$$$

# Deshabilitar verificador de compatibilidad
spring.cloud.compatibility-verifier.enabled=false